# URL of the Railway backend — required in production, leave unset in development
# (Vite's dev server proxy handles /api → localhost:3001 automatically)
VITE_API_URL=https://your-railway-app.railway.app
